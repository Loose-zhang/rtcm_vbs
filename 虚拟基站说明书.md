# rtcm_vbs_new.py 使用说明书

## 目录

1. [概述](#1-概述)
2. [工作原理](#2-工作原理)
3. [依赖环境](#3-依赖环境)
4. [快速上手](#4-快速上手)
5. [配置参数详解](#5-配置参数详解)
6. [运行模式](#6-运行模式)
   - [单基站模式 (single)](#61-单基站模式-single)
   - [双基站模式 (dual)](#62-双基站模式-dual)
7. [消息处理规则](#7-消息处理规则)
8. [核心模块说明](#8-核心模块说明)
9. [日志与监控](#9-日志与监控)
10. [常见问题](#10-常见问题)
11. [技术参考](#11-技术参考)

---

## 1. 概述

`rtcm_vbs_new.py` 是一个 **RTCM3 虚拟基站 (Virtual Base Station, VBS) 插值器**。

其核心功能是：从 TCP 端口读取真实基准站的 RTCM3 数据流，对基站位置和卫星观测值进行几何一致的坐标平移，生成一个位于**指定虚拟位置**的基准站数据流，并重新向外广播，供移动站（Rover）或 RTKLIB/rtkrcv 使用。

**典型应用场景：**

- 基准站离作业区域较远，需要将其"搬移"到更近的虚拟位置以提升 RTK 定位精度。
- 从两个物理基准站合并，合成一个覆盖更广或信号质量更好的虚拟站。
- 网络 RTK 中间件，在 NTRIP caster 与 RTKLIB 之间插入坐标变换层。

---

## 2. 工作原理

脚本对 RTCM3 数据流施加**两层修正**，以保证虚拟基站数据的几何一致性：

### 第一层：位置修正（MSG 1005 / 1006）

读取 1005/1006 消息中的天线参考点（ARP）ECEF 坐标，按照配置的 **北/东/天（N/E/U）偏移量**平移到虚拟位置，并重写报文中的坐标字段，重新计算 CRC-24Q。

```
真实基站 ECEF → 大地坐标 → 加 N/E/U 偏移 → 新大地坐标 → 虚拟 ECEF
```

### 第二层：观测量修正（MSM4/5/6/7）

对每颗卫星，利用广播星历（来自 MSG 1019/1042/1045/1046）计算卫星的瞬时 ECEF 位置，然后计算精确的**几何距离差**：

```
Δρ = dist(卫星, 虚拟基站) − dist(卫星, 真实基站)
```

将此修正量叠加到 MSM 帧中的**粗测距整毫秒（rough_int）** + **粗测距余量（rough_mod）** + **精细伪距（fine_PR）** + **精细载波相位（fine_PH）** 字段上。修正为原位（in-place）字节修改，不重建帧结构，避免与 rtkrcv 解码器的兼容性问题。

---

## 3. 依赖环境

| 依赖 | 说明 |
|------|------|
| Python 3.10+ | 需支持 `str \| None` 类型注解语法 |
| `pyrtcm` | RTCM3 解析库（`pip install pyrtcm`） |
| 标准库 | `socket`, `threading`, `struct`, `math`, `logging`, `copy`, `time` |

安装 `pyrtcm`：

```bash
pip install pyrtcm
```

---

## 4. 快速上手

### 步骤一：启动 RTCM 源（以 str2str 为例）

**主观测流（含 MSM + 1005/1006）：**
```bash
str2str -in ntrip://用户名:密码@播发器地址/挂载点 -out tcpsvr://:50001
```

**可选：独立星历流（含 1019/1042/1045/1046）：**
```bash
str2str -in ntrip://用户名:密码@播发器地址/星历挂载点 -out tcpsvr://:50010
```

### 步骤二：配置脚本底部的参数

打开 `rtcm_vbs_new.py`，找到文件末尾的 `── Configuration ──` 区块，按需修改参数（详见[第 5 节](#5-配置参数详解)）。

### 步骤三：运行脚本

```bash
python rtcm_vbs_new.py
```

### 步骤四：接收端接入

将移动站软件（RTKLIB、rtkrcv 等）的基准站数据源指向：

```
tcp://127.0.0.1:50002
```

---

## 5. 配置参数详解

所有配置均位于文件末尾 `if __name__ == "__main__":` 块内。

### 通用参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `MODE` | `"single"` \| `"dual"` | 运行模式，单基站或双基站 |
| `OUTPUT_PORT` | int | VBS 输出 TCP 端口（默认 `50002`） |

---

### 单基站模式参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `SOURCE_HOST` | `"127.0.0.1"` | RTCM 源主机地址 |
| `SOURCE_PORT` | `50001` | RTCM 源端口 |
| `OFFSET_NORTH` | `1000` m | 虚拟站相对真实基站的**北向**偏移量（正北为正） |
| `OFFSET_EAST` | `500.0` m | **东向**偏移量（正东为正） |
| `OFFSET_UP` | `50` m | **天向**偏移量（向上为正） |
| `EPHEM_HOST` | `"127.0.0.1"` | 独立星历流主机（设为 `None` 禁用） |
| `EPHEM_PORT` | `50010` | 独立星历流端口（设为 `None` 禁用） |

---

### 双基站模式参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `HOST_A` / `PORT_A` | `127.0.0.1:50001` | 主基站（A）TCP 地址 |
| `HOST_B` / `PORT_B` | `127.0.0.1:50003` | 辅助基站（B）TCP 地址 |
| `VBS_LAT` | `22.937764` | 虚拟站目标纬度（WGS-84，度） |
| `VBS_LON` | `113.206806` | 虚拟站目标经度（WGS-84，度） |
| `VBS_ALT` | `120.253` | 虚拟站目标椭球高（米） |
| `EPOCH_WINDOW_MS` | `40.0` | 等待 B 站同一历元数据的最长时间（毫秒） |
| `EPHEM_HOST` / `EPHEM_PORT` | `127.0.0.1:50010` | 可选共享星历流（两站共用） |

---

## 6. 运行模式

### 6.1 单基站模式 (single)

**数据流拓扑：**

```
NTRIP/TCP 源
     │  (RTCM3: 1005/1006 + MSM4-7 + 1019/1042/1045/1046)
     ▼ :50001
┌─────────────────────────────┐
│  rtcm_vbs_new.py            │
│  ① 接收 1005/1006           │
│     → 位置平移 N/E/U        │
│  ② 接收 1019/1042/1045/1046 │
│     → 填充星历缓存           │
│  ③ 接收 MSM4-7              │
│     → 精确距离差修正         │
│  ④ 过滤不支持星座            │
└─────────────────────────────┘
     │  (修正后 RTCM3 VBS 流)
     ▼ :50002
  移动站 / rtkrcv
```

**启动序列（冷启动保护）：**

脚本在收到第一条星历（1019/1042/1045/1046）之前，**不会**修改 1005/1006 和 MSM 帧，直接透传原始数据，保证冷启动阶段数据几何一致。一旦星历就绪，立即切换为全修正模式并打印日志。

---

### 6.2 双基站模式 (dual)

**数据流拓扑：**

```
基站 A (:50001)   基站 B (:50003)
      │                   │
      ▼                   ▼
  reader-A 线程      reader-B 线程
      │                   │
      └──────┬────────────┘
             ▼
      merger/output 循环（主线程）
      ┌─────────────────────────────────┐
      │  对每个 GPS 历元：               │
      │  • 仅 A 有某星座 → A 修正后输出  │
      │  • 仅 B 有某星座 → B 修正后输出  │
      │  • A、B 都有   → 解析+修正+合并  │
      │    (CNR 最高的卫星优先)           │
      └─────────────────────────────────┘
             │
             ▼ :50002
          移动站 / rtkrcv
```

**历元对齐策略：**

- 主循环每 5 ms 轮询一次缓冲区。
- 收到 A 站某历元数据后，等待最多 `EPOCH_WINDOW_MS`（默认 40 ms）以收集 B 站同历元数据。
- 超时后以 A 站单站数据输出，不丢弃历元。

**合并策略（同星座双站）：**

| 情况 | 处理 |
|------|------|
| 仅 A 有该卫星 | 使用 A 的观测值 |
| 仅 B 有该卫星 | 使用 B 的观测值 |
| A、B 均有该卫星 | 取**平均 CNR 更高**的一站的观测值 |
| 信号类型掩码 | A 和 B 的信号掩码取**并集** |

---

## 7. 消息处理规则

| RTCM 消息类型 | 处理方式 |
|---------------|----------|
| **1005 / 1006** | 修改 ARP ECEF 坐标为虚拟站坐标，重算 CRC |
| **1019** (GPS 星历) | 解析并缓存到 EphemerisStore |
| **1042** (BeiDou 星历) | 解析并缓存 |
| **1045 / 1046** (Galileo F/NAV, I/NAV) | 解析并缓存 |
| **MSM4–7 (GPS/GAL/BDS)** | 修正粗测距 + 精细伪距/相位字段，重算 CRC |
| **MSM (GLONASS/SBAS/QZSS)** | **丢弃**（不支持的星座） |
| **1020** (GLONASS 星历) | **丢弃** |
| **1230** (GLONASS 码-相位偏差) | **丢弃** |
| **1001–1004** (GLONASS 传统观测) | **丢弃** |
| **1009–1012** (GLONASS 传统观测) | **丢弃** |
| 其他未知消息 | 直接透传 |

> **支持星座：GPS、Galileo、BeiDou（仅这三种）**

---

## 8. 核心模块说明

### 8.1 坐标转换

| 函数 | 功能 |
|------|------|
| `ecef_to_geodetic(x, y, z)` | ECEF → 大地坐标（纬/经/高），Bowring 迭代法 |
| `geodetic_to_ecef(lat, lon, alt)` | 大地坐标 → ECEF |
| `apply_ned_offset(lat, lon, alt, dn, de, du)` | 大地坐标加 N/E/U 偏移，返回新大地坐标 |

---

### 8.2 位帧操作

| 函数 | 功能 |
|------|------|
| `_get_bits(data, start, n)` | 从字节串中按 MSB-first 提取 n 位无符号整数 |
| `_set_bits(ba, start, n, value)` | 将有符号/无符号整数以 MSB-first 写入 bytearray |
| `_as_signed(val, bits)` | 将无符号整数重新解释为有符号补码整数 |
| `crc24q(data)` | 计算 CRC-24Q 校验值 |
| `_recompute_crc(frame)` | 返回重算 CRC 后的 RTCM3 帧副本 |

---

### 8.3 位置报文修补

**`patch_arp_position(raw_frame, dn, de, du)`**

修改 1005/1006 帧中的 ARP ECEF 字段（DF025/DF026/DF027），清除 GLONASS 系统标志位（DF023），重算 CRC，返回新帧字节。

---

### 8.4 星历存储（EphemerisStore）

```python
ephem = EphemerisStore()
ephem.update(mtype, parsed_msg)        # 从 pyrtcm 解析对象导入星历
pos = ephem.sat_ecef(constl, sv, tow)  # 计算卫星 ECEF 位置
```

支持星座及对应 RTCM 消息：

| 星座 | 消息 | 轨道类型 |
|------|------|----------|
| GPS | 1019 | MEO |
| Galileo | 1045 (F/NAV) / 1046 (I/NAV) | MEO |
| BeiDou | 1042 | MEO / IGSO / GEO |

**BDS GEO 卫星特殊处理：** PRN 1–5 及 59–64 使用倾斜参考系旋转（−5° 倾角），完全对应 RTKLIB `ephemeris.c` 的 `eph2pos()` 实现。

---

### 8.5 MSM 观测量修补

**`patch_msm_inplace(raw_frame, base_xyz, vbs_xyz, ephem)`**

核心修正流程：

1. 解析 MSM 帧头，读取卫星掩码、信号掩码、小区掩码。
2. 记录粗测距字段（`rough_int` 8位、`rough_mod` 10位）和精细字段（`fine_pr`、`fine_ph`）的**位偏移地址**。
3. 对每颗卫星：
   - 从 `EphemerisStore` 取当前历元的卫星 ECEF 坐标（开普勒积分）。
   - 计算精确距离差 `Δρ = dist(卫星, VBS) − dist(卫星, 真实站)`。
4. 将 `Δρ` 拆分为粗测距修正（以 1/1024 ms 步长量化）和残差修正（写入 fine_pr / fine_ph）。
5. 无星历的卫星：将 `rough_int` 置为 `255`（RTCM 无效哨兵），rtkrcv 将丢弃此卫星。
6. 重算 CRC-24Q，返回修正后的帧字节。

**MSM 子类型的字段宽度（位数）：**

| 子类型 | fine_pr | fine_ph | lock | cnr | rate |
|--------|---------|---------|------|-----|------|
| MSM4   | 15      | 22      | 4    | 6   | 无   |
| MSM5   | 15      | 22      | 4    | 6   | 15   |
| MSM6   | 20      | 24      | 10   | 10  | 无   |
| MSM7   | 20      | 24      | 10   | 10  | 15   |

---

### 8.6 双基站专用函数

| 函数 | 功能 |
|------|------|
| `parse_msm_frame(raw_frame)` | 将 MSM 帧完整解析为 Python dict |
| `correct_parsed_msm(p, base_xyz, vbs_xyz, ephem)` | 对解析后的 MSM dict 施加 VBS 修正（深拷贝） |
| `merge_parsed_msm(ca, cb)` | 合并两个已修正的 MSM dict（CNR 优先策略） |
| `build_msm_frame(parsed)` | 将 MSM dict 重新编码为完整 RTCM3 帧（含 CRC） |
| `set_arp_ecef(raw_frame, x, y, z)` | 精确设置 1005/1006 帧的 ARP ECEF 坐标 |

---

### 8.7 TCP 广播服务器（TcpBroadcastServer）

- 监听 `0.0.0.0:<OUTPUT_PORT>`，接受多个 TCP 客户端同时连接。
- `send(data)` 将数据广播给所有已连接客户端；断开的客户端自动从列表移除。
- `TCP_NODELAY` 已启用，降低输出延迟。

---

### 8.8 独立星历馈送线程（start_ephem_feeder）

如果主 NTRIP 挂载点不包含星历，可配置独立的星历流端口。函数启动一个后台守护线程，连接到指定 TCP 地址，持续解析 1019/1042/1045/1046 消息并写入 `EphemerisStore`。连接中断后自动重连（间隔 5 秒）。

---

## 9. 日志与监控

脚本使用标准 Python `logging`，输出到 `stdout`，时间戳精确到秒。

### 关键日志条目说明

| 日志前缀 | 含义 |
|----------|------|
| `Base ARP :` / `VBS ARP :` | 首次 1005/1006 修正时打印真实/虚拟站坐标 |
| `MSG1xxx GPS/GAL/BDS MSM%d: valid corrections …` | 每 15 秒汇报一次各星座的有效修正卫星数 |
| `[DETAIL] GPS SV%02d: rough_int=…` | 每星座**第一次**修正时的卫星级别详情 |
| `[VALIDATE] %s frame OK: … bytes changed` | 前 5 帧逐帧结构验证结果 |
| `[ROUNDTRIP] %s: … verified OK` | 前 5 帧的往返精度验证（PR<0.02m，PH<0.002m） |
| `Stats — total: … MSM patched: …` | 每处理 500 帧打印一次统计摘要 |
| `Startup phase: no ephemeris yet; deferring …` | 冷启动阶段尚无星历，延迟修正 |
| `Ephemeris ready; enabling VBS 1005/1006 patch` | 星历就绪，开始正式修正 |

---

## 10. 常见问题

**Q：接收端长时间没有有效定位，日志显示"valid corrections 0/N"？**

A：脚本未收到足够的星历。检查：
- 主 RTCM 流是否包含 MSG 1019/1042/1045/1046；
- 若已配置 `EPHEM_PORT`，确认该端口上的 str2str 已正常运行；
- 冷启动等待 1~2 分钟，等足够的星历积累。

---

**Q：日志出现"[ROUNDTRIP] encoding errors detected"？**

A：理论上不应发生。如出现，说明 fine_pr/fine_ph 修正后的总观测量存在偏差，可能是极端卫星几何或溢出情况。验证计数仅限于前 5 帧，不影响后续正常运行；可提升日志级别到 `DEBUG` 进一步排查。

---

**Q：双基站模式 B 站数据始终等到超时才输出？**

A：检查：
- B 站的 str2str 是否正常运行；
- B 站 TCP 端口是否与 `PORT_B` 一致；
- 适当增大 `EPOCH_WINDOW_MS`（如调整到 80–100 ms）。

---

**Q：如何关闭独立星历流功能？**

A：将 `EPHEM_PORT = None` 即可禁用。脚本将只依赖主 RTCM 流中的内嵌星历消息。

---

**Q：支持哪些 GNSS 星座？**

A：当前版本仅支持 **GPS、Galileo、BeiDou**。GLONASS 以及 SBAS、QZSS 的 MSM 和星历均被过滤丢弃，以保证 VBS 几何一致性。

---

## 11. 技术参考

| 参考标准/代码 | 说明 |
|---------------|------|
| RTCM Standard 10403.x | MSG 1005/1006 位字段布局，MSM4/5/6/7 帧结构 |
| WGS-84 | 坐标系参数：`a=6378137 m`，`f=1/298.257223563` |
| RTKLIB `ephemeris.c eph2pos()` | Keplerian 卫星位置计算，BDS GEO 特殊处理 |
| RTKLIB `rtklib.h` | 光速 `C=299792458 m/s`，地球自转角速率 |
| CRC-24Q | RTCM3 帧完整性校验算法（多项式 `0x1864CFB`） |

---

*文档生成时间：2026-04-14*
